import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Empty config - let Next.js 16 use Turbopack by default
  // Turbopack handles client/server separation automatically
};

export default nextConfig;
